_schema-version: "2.0.0"
ID: "ui5-reference"
version: 1.0.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "ui5-reference"
    type: html5
    path: .
    parameters:
      version: 1.0.0
    build-parameters:
      builder: grunt
      build-result: dist
